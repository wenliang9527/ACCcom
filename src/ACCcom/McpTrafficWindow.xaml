<Window x:Class="ACCcom.McpTrafficWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ACCcom"
        Title="MCP Traffic" Height="420" Width="640"
        MinHeight="260" MinWidth="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource BgBaseBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border x:Name="TitleBar" Grid.Row="0" Background="{DynamicResource BgSurfaceBrush}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left"
                           Text="MCP Traffic — shared serial log"
                           Foreground="{DynamicResource InkSecondaryBrush}"
                           FontSize="12" FontWeight="Medium"
                           VerticalAlignment="Center" Margin="12,0,0,0" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="&#xE74C;" Style="{StaticResource TitleBarButton}" FontSize="11" Width="46" Height="32" Click="Clear_Click" ToolTip="Clear log" />
                    <Button Content="&#x2715;" Style="{StaticResource TitleBarCloseButton}" FontSize="11" Width="46" Height="32" Click="TitleBarClose_Click" ToolTip="Close" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="6,2" Background="{DynamicResource BgSurfaceBrush}" CornerRadius="4" Margin="0,0,0,4">
                <TextBlock x:Name="HintText" FontSize="11" Foreground="{DynamicResource InkTertiaryBrush}"
                           Text="Live view of serial traffic sent/received by the MCP (AI) tools. The MCP process appends to mcp-traffic.jsonl and this window tails it." TextWrapping="Wrap" />
            </Border>

            <ListView x:Name="TrafficList" Grid.Row="1"
                      Background="{DynamicResource BgSurfaceBrush}"
                      BorderThickness="0"
                      FontFamily="Consolas" FontSize="12"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Time" Width="90" DisplayMemberBinding="{Binding Time}" />
                        <GridViewColumn Header="Dir" Width="34" DisplayMemberBinding="{Binding Direction}" />
                        <GridViewColumn Header="Tool" Width="90" DisplayMemberBinding="{Binding Tool}" />
                        <GridViewColumn Header="Text / Hex" Width="420" DisplayMemberBinding="{Binding Payload}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>