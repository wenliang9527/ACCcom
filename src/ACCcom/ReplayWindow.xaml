<Window x:Class="ACCcom.ReplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ACCcom"
        Title="{Binding [Replay.Title], Source={x:Static local:LanguageManager.Instance}}" Height="160" Width="420"
        MinHeight="140" MinWidth="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="{DynamicResource BgBaseBrush}"
        Closing="Window_Closing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar (no min/max since ResizeMode=NoResize) -->
        <Border x:Name="TitleBar" Grid.Row="0" Background="{DynamicResource BgSurfaceBrush}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left"
                           Text="{Binding [Replay.Title], Source={x:Static local:LanguageManager.Instance}}"
                           Foreground="{DynamicResource InkSecondaryBrush}"
                           FontSize="12" FontWeight="Medium"
                           VerticalAlignment="Center" Margin="12,0,0,0" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="&#x2715;" Style="{StaticResource TitleBarCloseButton}" FontSize="11" Width="46" Height="32" Click="TitleBarClose_Click" ToolTip="{Binding [TitleBar.Close], Source={x:Static local:LanguageManager.Instance}}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Status text -->
        <TextBlock x:Name="StatusText" Grid.Row="0"
                   Text="{Binding [Replay.Ready], Source={x:Static local:LanguageManager.Instance}}" FontSize="13"
                   Foreground="{DynamicResource InkPrimaryBrush}" />

        <!-- Progress bar -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="ProgressBar" Grid.Column="0"
                         Height="20" Minimum="0" Maximum="1"
                         Foreground="{DynamicResource AccentBrush}"
                         Background="{DynamicResource BgElevatedBrush}" />
            <TextBlock x:Name="ProgressText" Grid.Column="1"
                       Text="0/0" FontSize="11" FontFamily="Consolas"
                       Foreground="{DynamicResource InkSecondaryBrush}"
                       VerticalAlignment="Center" Margin="8,0,0,0" />
        </Grid>

        <!-- Controls -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="PlayPauseButton" Content="{Binding [Replay.Play], Source={x:Static local:LanguageManager.Instance}}"
                    Width="80" Height="28" Margin="0,0,8,0"
                    Click="PlayPause_Click"
                    Style="{StaticResource OutlineButton}" />
            <Button Content="{Binding [Replay.Stop], Source={x:Static local:LanguageManager.Instance}}" Width="80" Height="28" Margin="0,0,16,0"
                    Click="Stop_Click"
                    Style="{StaticResource OutlineButton}" />

            <TextBlock Text="{Binding [Replay.Speed], Source={x:Static local:LanguageManager.Instance}}" VerticalAlignment="Center"
                       Foreground="{DynamicResource InkSecondaryBrush}"
                       Margin="0,0,6,0" />
            <ComboBox x:Name="SpeedCombo" Width="80" Height="26"
                      SelectedIndex="1"
                      SelectionChanged="Speed_SelectionChanged">
                <ComboBoxItem Content="{Binding [Replay.Speed05x], Source={x:Static local:LanguageManager.Instance}}" Tag="0.5" />
                <ComboBoxItem Content="{Binding [Replay.Speed1x], Source={x:Static local:LanguageManager.Instance}}" Tag="1.0" />
                <ComboBoxItem Content="{Binding [Replay.Speed2x], Source={x:Static local:LanguageManager.Instance}}" Tag="2.0" />
                <ComboBoxItem Content="{Binding [Replay.Speed5x], Source={x:Static local:LanguageManager.Instance}}" Tag="5.0" />
                <ComboBoxItem Content="{Binding [Replay.SpeedInstant], Source={x:Static local:LanguageManager.Instance}}" Tag="0" />
            </ComboBox>
        </StackPanel>
        </Grid>
    </Grid>
</Window>
