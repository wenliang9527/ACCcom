# 可视化分析

ACCCOM 提供实时绘图、统计仪表盘、数据对比和差异分析四类可视化工具，支持从原始数据到结构化洞察的完整分析链路。

## 实时绘图

`PlotWindow` 实时绘制串口数据波形：

- **实时更新**：基于 `PlotViewModel` 驱动数据更新
- **自定义配置**：支持自定义字段偏移量、数据类型、缩放
- **连续数据流**：适合传感器读数、电流曲线等连续数据流的可视化分析

## 统计仪表盘

`StatsWindow` 串口数据统计可视化：

- **实时指标**：基于 `StatsViewModel` 展示接收速率、帧率、错误率等指标
- **数字读数**：指标以数字读数的形式实时刷新

## 数据对比

`CompareWindow` 对比两个文本文件：

- **行级对比**：逐行比较两个文件的每一行（短文件自动补空行）
- **差异高亮**：不同的行以背景色高亮，两侧同步滚动
- **统计摘要**：显示总行数、相同行数与不同行数

> Hex 字节级对比使用工具栏的"对比"按钮（DiffWindow）；文件行级对比使用"对比文件"按钮。

## 数据差异

`DiffWindow` 数据差异分析：

- **差异可视化**：两组数据之间的差异可视化展示
- **并排查看**：支持并排查看和差异高亮

## 相关文档

- [数据管理](../guide/data-management.md)
- [串口操作指南](../guide/serial.md)
